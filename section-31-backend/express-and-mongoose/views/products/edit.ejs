<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit <%= product.name %> </title>
</head>
<body>
    <h1>Edit page for - <%= product.name %></h1>
    <form action="/products/<%= product._id %>?_method=PATCH" method="POST">
        <label for="price">new price</label>
        <input type="number" name="price" id="price" placeholder="price" value="<%= product.price %>" >
        <label for="category">new category</label>
        <select name="category" id="category">
            <option value="fruit" <%= product.category==='fruit' ? "selected" : ""  %> >fruit</option>
            <option value="vegetable" <%= product.category==='vegetable' ? "selected" : ""  %>>vegetable</option>
            <option value="dairy" <%= product.category==='dairy' ? "selected" : ""  %>dairy</option>
        </select>
        <input type="submit" value='submit'>
        <button type="button" onclick="location.href='/products/<%=product._id%>'">go back</button>
    </form>
</body>
</html>